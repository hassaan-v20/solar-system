# Asset drop zone

Drop downloaded model files (`.glb` preferred) here, then tell me roughly what each is.
I'll inspect (size/orientation/center), rename, move to the right `models/` subfolder,
and wire it into the game.

Prefer: **GLB**, ~1–2k textures (a few MB), **CC0 or CC-BY** license, low/mid poly.
Dropping into `~/Downloads` works too — just say what you added.
